# Pinned. An add-on that builds on the user's own machine should build the same
# way in six months as it does today.
#
# All five have musllinux wheels for aarch64 and x86_64, so nothing is compiled
# during the install and a Raspberry Pi does not spend twenty minutes on it.
aiohttp==3.14.3
cryptography==46.0.7
paho-mqtt==2.1.0
pyserial==3.5
PyYAML==6.0.3
